# Helm Deployments

This folder is reserved for Helm charts or chart composition.

The scaffold does not define a production deployment contract yet. Add Helm resources only when the runtime boundary and operational ownership are explicit.

